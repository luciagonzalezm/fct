export class Pregunta {
    constructor(public id: number, public enunciado: string, public respuesta1: string, public resultado1: boolean, public respuesta2: string, public resultado2: boolean, public respuesta3: string, public resultado3: boolean) {
    }
}
