export class Supervisor {
    constructor(public id: number, public role: string, public name: string, public firstName: string, public lastName: string, public department: number, public email: string, public username: string, public password: string) {
    }
}
